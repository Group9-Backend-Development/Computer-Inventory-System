<section class="page">
  <h1>{{title}}</h1>
  <p>Form fields: item ID, serial, model, brand, category, status, date acquired.</p>
  {{#if item}}
    <p>Editing reference: <code>{{item.id}}</code></p>
  {{/if}}
</section>
