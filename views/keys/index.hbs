<section class="space-y-4 rounded-2xl border border-sky-200 bg-white p-6 shadow-sm">
  <h1 class="text-2xl font-bold text-slate-800">{{title}}</h1>
  <p class="text-slate-600">Generate, list, and revoke API keys (hashed at rest; raw key shown once).</p>
  <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 text-sm text-slate-700">
    <p class="font-semibold text-sky-700">Security note</p>
    <p class="mt-1">Always copy newly generated keys immediately. Plain values are displayed only once.</p>
  </div>
</section>
